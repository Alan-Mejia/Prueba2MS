server:
  port: 9090

spring:
  cloud:
    config:
      server:
        git:
          default-label: multiple-instances
          uri: https://github.com/Alan-Mejia/Prueba2MS
          search-paths: config-data
  application:
    name: config-service
